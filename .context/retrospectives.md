### ICON-0085: Isolate @architect & @planner — remove a special case by making it uniform; scope-supersede a prior ADR's sub-decision, leaving its prose intact
- **Avoid**: The design doc had an internal inconsistency — §7a PROSE said the @architect downgrade tier is `default`, but the §7b literal site-edit table put it under a `→ basic` column. A coder implemented the site-edit verbatim but FLAGGED the mismatch, and @reviewer caught it (an architecture check isn't Haiku-mechanical → `default`). A design's prose rationale and its literal site-edit values can drift; when implementing a literal site-edit, cross-check it against the design's own prose reasoning and flag drift — the flag is what saved it. Same "an artifact's restatements can drift" class as verify-design-claims-against-artifacts, applied to a design doc's own internals.
- **Avoid**: Shipped a FALSE harness-capability premise that passed BOTH design and @reviewer: the design claimed an isolated sub-agent "cannot clarify live" and replaced @planner's clarification with report-only open questions + a per-agent `ask_user`-precedence override. Wrong — a sub-agent CAN use `ask_user`; isolation is a separate context window + a returned report, NOT loss of the user-input channel. The maintainer caught it post-review. Before designing AROUND a claimed harness limitation ("isolated agents can't X"), VERIFY the harness actually imposes it — do not infer a capability loss from the isolation mechanism. Same assume-vs-verify class as ICON-0073 (verify external-tool behavior by execution, not docs). Corrected on the open PR: isolated @planner/@architect retain `ask_user`; the report open-questions are a COMPLEMENT (assumptions + manager-facing questions); only the inline mid-run manager dialogue genuinely goes away.
- **Repeat**: (1) Removed a special case by making the dispatch model UNIFORM: @architect/@planner were the only two inline agents, so isolating them collapsed the inline/shared-context category to zero members and let the whole concept be removed (one dispatch model, not two) — reusing the existing warmstart template + both agents' existing report contracts + the tier machinery, adding exactly ONE new field (`### Architecture`, the cold-resume slice that standards/domains didn't already cover; ADR-008-lean). (2) Scope-superseding ADR: the isolation model had NO ADR, so created ADR-015 as the record AND scope-superseded only ADR-014's inline carve-out — `**Supersedes**`/`**Superseded-by**` bold-fields both directions with the scope noted ("carve-out only — 014's core stands"), leaving ADR-014's Decision prose INTACT as the historical superseded record. (3) The coupling-ownership pattern (ICON-0084 retro) validated: ICON-0085 owned reconciling the carve-out ICON-0084 introduced, contained in the same files, no churn.
- **Updated**: `skills/context-document-guidelines/SKILL.md` § Related Section (graph seam) → ADR supersede bold-fields — extended the ADR cross-reference convention with the **scope-supersede (partial)** pattern: a new ADR resolving only a PRIOR ADR's specific sub-decision sets `**Supersedes**: ADR-NNN (<scope> only — remainder stands)` on the new ADR and `**Superseded-by**: ADR-MMM (<scope> only)` on the old — the scope noted in BOTH directions — and leaves the partially-superseded ADR's `**Status**`/Decision prose INTACT as the historical record (don't mutate it), citing ICON-0085 (additive shipped-skill doc, no `context_template/` touch → no iconrc bump). Also promoted the false-harness-capability lesson (post-review correctness fix) to `.context/standards/skill-decomposition/verify-design-claims-against-artifacts.md` as a harness-capability corollary — a claimed HARNESS/TOOL limitation ("a sub-agent can't use X", "isolation removes Y") is a hypothesis until verified against the actual harness contract (occurrence count five → six, extending the ICON-0073 external-tool corollary). The other lessons (design-prose-vs-literal-site-edit drift flagged at implementation; removing a special case by making the dispatch model uniform + ADR-008-lean single new field; ICON-0084 coupling-ownership validated) stay retro-only as task-specific applications. This entry inserted via `append-retrospective-entry.sh` (10 → 10 at cap, pruned 1 oldest entry: ICON-0075, archived to `.context/retrospectives-archive.md`).

### ICON-0084: Model-aware delegation — a queued follow-up owns reconciling the assumption it changes; ADRs use bold-fields, not `## Related`
- **Avoid**: An @coder gave ADR-014 a `## Related` footer (links to ADR-004/008), but the ICON-0081 F1 convention routes ADR cross-references to the `**Supersedes**`/`**Superseded-by**` bold-fields + plain-prose `ADR-NNN` refs, NOT a `## Related` footer (that seam is for CONTENT docs — domains/standards/workflows/etc.). The convention was established for the generator (impl-root emission) but not stated where an ADR *author* sees it, so it recurred; @reviewer caught it. Fix: make the ADR cross-reference convention explicit in `context-document-guidelines` (the seam authority) so an ADR author doesn't reach for `## Related`.
- **Repeat**: (1) When the user queued ICON-0085 (isolate @planner/@architect) mid-way through ICON-0084 — which depends on those agents being INLINE (they inherit the session model; the tier rule carves them out) — the coupling was surfaced explicitly and the reconciliation assigned to ICON-0085's natural scope (it already touches the same manager / routing-guide / ADR files), rather than churning in-flight ICON-0084 or discarding its work. The follow-up that CHANGES an assumption owns updating what depended on it. (2) ADR-008-lean placement held: the required field + a one-line rule in the always-loaded manager (+111w) / PM (+70w), the full task→tier mapping in the ON-DEMAND `manager-routing-guide` (+569w, unbudgeted) — a small always-loaded delta right after a terseness pass. (3) Portability (ADR-004) for a harness-specific capability = express the portable CONCEPT (tier), map to model ALIASES (not pinned IDs, so the table rarely churns), and degrade gracefully (advisory where a harness lacks per-subagent model control) so delegation never fails.
- **Updated**: `skills/context-document-guidelines/SKILL.md` § Related Section (graph seam) → ADR supersede bold-fields — added a bullet making the ADR cross-reference convention explicit at the seam authority where an ADR *author* looks: an ADR records supersede relationships in the `**Supersedes**` / `**Superseded-by**` bold-fields and references OTHER ADRs in plain prose as `ADR-NNN`, and does **not** get a `## Related` footer (that footer seam is for content docs only — `domains/`, `standards/`, `workflows/`, `architecture/`, `testing/`, `styling/`), citing ICON-0081 F1 + ICON-0084 (additive shipped-skill doc, no `context_template/` touch → no iconrc bump). The other lessons (a queued follow-up owns reconciling the assumption it changes; ADR-008-lean always-loaded placement of the tier rule; portable-concept + model-aliases + graceful-degradation for a harness-specific capability) stay retro-only as task-specific applications. This entry inserted via `append-retrospective-entry.sh` (10 → 10 at cap, pruned 1 oldest entry: ICON-0074, archived to `.context/retrospectives-archive.md`).

### ICON-0083: Whole-tree terseness pass — for a concision task the guardrail is BEHAVIOR preservation, not verbatim wording; set a word-count target
- **Avoid**: The first pass was far too CONSERVATIVE (manager only −2%) because the guardrail treated anti-rationalization tables, Hardcoded lists, and contract prose as "keep VERBATIM" — which caps a terseness task at the thin unprotected-prose surface and defeats its purpose. The maintainer corrected ("another agent reduced manager 15-20% without impacting behavior"). For a concision/terseness task the guardrail is **BEHAVIOR/RELIABILITY preservation, not verbatim-WORDING preservation**: every rule/trigger/gate/table-row must still BIND identically, but the words — including inside tables and contracts — should shrink hard. A concrete word-count TARGET (~15-20% on verbose always-loaded files) calibrates better than a fidelity metaphor ("JPEG-95") alone, which collapsed into "barely touch anything." Only truly literal string-matched tokens (hook/gate markers, script-parity byte-identity, iconrc schema keys, cited `.context/` paths, `## Related`/`**Supersedes**` seam fields, JSON field names) stay byte-verbatim.
- **Repeat**: (1) Front-loaded inventory → disjoint-batch plan + a per-category do-not-trim guardrail + a gate cheat-sheet let 6 batches run conflict-free in one worktree (ICON-0080 pattern). (2) For instruction content with no test runner, "reliability preserved" is VERIFIABLE: per-batch invariants (discipline-table row-counts unchanged, `description:` frontmatter byte-identical, parity scripts byte-unchanged, a real pre-commit hook run) + two adversarial reviewers CONFIRMING each claim line-by-line and checking meaning-not-just-words. Both approved a 98-file, ~−4,800-word diff with zero behavior drift. (3) Stopped manager at −13.1% rather than de-dup the tiered close-gate restatement (Step 6 + Hardcoded + anti-rat) — surfaced the reliability-reinforcement-vs-size tradeoff to the user instead of unilaterally cutting reinforcing redundancy.
- **Updated**: Promoted the concision-calibration lesson to a NEW standard `.context/standards/terseness-calibration.md` (guardrail = behavior-not-verbatim preservation for a concision task, with the byte-verbatim token classes enumerated; a ~15-20% word-count TARGET, distinct from ADR-008's growth ceiling; the no-test-runner verification invariants — table row-counts, `description:` byte-identity, parity-script identity, gates green, adversarial meaning-not-words review; and the don't-de-dup-reinforcing-redundancy-without-sign-off rule), citing ICON-0083 and ICON-0033/ADR-008 as prior art; added its `rules-index.md` Standards row so it is index-reachable (no `## Related` block → no orphan; ADR-008/ICON-0033 cited by name, dead-ref-safe). The other lessons (front-loaded disjoint-batch planning with a per-category guardrail + gate cheat-sheet; per-batch reliability invariants + dual adversarial reviewers; surfacing the reinforcement-vs-size tradeoff rather than unilaterally cutting) stay retro-only as task-specific applications. This entry inserted via `append-retrospective-entry.sh`.

### ICON-0082: Session-per-phase (plan.md handoff hardening + phase-launcher generator) — a claimed safety BOUND is a hypothesis until you trace its enforcing mechanism fires in every mode
- **Avoid**: (1) The `generate-phase-launcher` skill ADVERTISED a bounded-retry guarantee ("cannot spin forever"), but nothing incremented the persisted `Attempts` field on failure — so the bare-cron realization re-invoked `claude -p` every tick FOREVER on a clean fail-closed STOP (Templates A/B were saved only by an in-process counter; CI by `needs:`). Structural validation missed it; @reviewer caught it by tracing the counter's increment path across every template/skill. A claimed safety/liveness bound (retry cap, rate limit, timeout) is a hypothesis until you trace the mechanism that ENFORCES it actually fires in EVERY execution mode — check the increment/enforcement path, not just the check site. (2) A pre-existing PowerShell bug silently broke the same bound in PS mode: `[int]::TryParse((… -replace '\D',''), [ref]$x)` — PowerShell parses the `-replace 'pat','repl'` commas as method-argument separators, giving TryParse the wrong arity (throws under StrictMode). Parenthesize any `-replace` used inside a method call's argument list.
- **Repeat**: (1) The "phase boundaries = commit points" design invariant eliminated a whole handoff gap item (uncommitted working-tree state) and turned the impossible self-embedded-SHA into a `Phase-Handoff:` commit trailer — choosing the right invariant simplifies more than serializing state. (2) The launcher-GENERATOR reframe (a portable skill that emits the harness-specific script from a `target-harness` config, rather than shipping a harness-locked launcher) resolved the ADR-004 portability tension the ICON-idiomatic way (skills that generate artifacts). (3) For a security-sensitive GENERATOR skill, a BEHAVIORAL pressure-test — give a fresh agent the skill + realistic pressure to violate its security rule ("make sure the next phase has the prior findings so context isn't lost") and check whether the artifact it produces still holds the guarantee — validates the SKILL's instructions, not just a structural check of one emitted sample. The agent refused to embed handoff text into the `-p` prompt and cited the skill's own security rule; paired with @reviewer's empirical probe (crafted malicious plan.md → directive stayed constant) that PROVED the injection guarantee rather than reasoning about it.
- **Updated**: Two promotions this pass. (1) `.context/standards/skill-decomposition/verify-design-claims-against-artifacts.md` — added ICON-0082 as the fifth occurrence on the verify-design-claims axis plus a "Claimed-bound corollary (ICON-0082)": a safety/liveness bound asserted by a spec or skill (retry cap, rate limit, timeout, "cannot spin forever") is a hypothesis until you trace the mechanism that ENFORCES it fires in every execution mode/path — verify the increment/enforcement site, not just the check; bumped the occurrence count (four → five) and precedents line. (2) `.context/standards/shell-portability.md` — added Rule 6, the PowerShell `-replace`-comma gotcha: inside a .NET method-call argument list, `-replace 'pattern','replacement'`'s two commas are parsed as method-argument separators (breaking `[int]::TryParse((… -replace '\D',''), [ref]$n)` under StrictMode), so wrap the `-replace` expression in its own parentheses `((… -replace '\D',''))`. The other lessons (the phase-boundaries-are-commit-points invariant; the launcher-generator reframe resolving the ADR-004 portability tension; the behavioral pressure-test of a security-sensitive generator skill) stay retro-only as task-specific applications. `.context/retrospectives.md` — this entry inserted via `append-retrospective-entry.sh` (10 → 10 at cap, pruned 1 oldest entry: ICON-0072, archived to `.context/retrospectives-archive.md`); also removed two stray `</content>`/`</invoke>` tag lines a prior append had left inside the ICON-0081 entry. The task's primary deliverables (the `generate-phase-launcher` skill + command, the phase-launcher templates, ADR-013, and the plan.md handoff hardening) are committed under the task itself.

### ICON-0081: Context knowledge graph (parser + `## Related`/ADR seam + fail-closed gate) — build the graph from signals that already exist; a fail-CLOSED gate needs a 3-value exit contract
- **Avoid**: Two traps. (1) The task premise assumed `[[wiki-links]]` were a traversable `.context/` cross-reference convention — a read-only Explore pass showed they are NOT (only bash test syntax / retrospective feedback-tags; real cross-refs are `[text](path)` links + by-name prose). Designing the graph on `[[links]]` would have built on nothing. Verify a referenced *convention/idiom* actually exists in the repo before designing on it — same class as ICON-0072's "verify a referenced artifact exists," extended from files to conventions. (2) A literal `[text](path)` EXAMPLE written in prose is parsed by `context-graph` as a REAL edge → dangling-ref false positive; hit independently in ADR-012 prose and in template example links. When a `.context/` doc must SHOW an example Markdown link, wrap it in a `<!-- pre-commit:dead-ref-ok-start/end -->` region (or show it without link syntax), or the graph parser + the new pre-commit gate flag it.
- **Repeat**: (1) Dependency-ordered phasing with disjoint-file parallelism: parser FIRST (reliability foundation) → seam/backfill ‖ wiring → ADR ‖ gate → gate committed LAST on a green tree; parallelizing only disjoint-directory phases stayed conflict-free (ICON-0080 pattern) and each phase's coder read the durable `design.md` as warmstart. (2) For the two reliability-critical surfaces, scenario-test in ISOLATION (throwaway git repos) BEFORE going live, then have @reviewer independently re-run an adversarial battery (ICON-0071/0075). The recipe that closed the ICON-0075 fail-OPEN class for a NEW fail-CLOSED gate: a 3-value exit contract (0 clean / 1 violation / 2 parser-error), invoked `|| { exit 1; }` (never `if script`), with "zero nodes on a non-empty tree ⇒ exit 2" so a parser bug cannot pass as clean. (3) @architect ruling to fix the graph MODEL (a directory-target rules-index row confers `covers` reachability to child files) instead of masking 6 genuinely-indexed files with orphan markers — fix the model, not the symptom. (4) Dogfooded the new seam on the task's own ADR-012, validated by the gate it introduced.
- **Updated**: Three promotions this pass. (1) `skills/context-document-guidelines/SKILL.md` § Related Section (graph seam) — added an authoring caveat that an illustrative/example `[text](path)` link written anywhere in a `.context/` content doc is parsed as a REAL `references` edge and flagged dangling by `--check`/pre-commit, so an example link must be wrapped in a `<!-- pre-commit:dead-ref-ok-start/end -->` region or shown without link syntax (additive shipped-skill doc, no `context_template/` touch → no iconrc bump). (2) `.context/standards/secure-coding.md` — added Rule 11 distinguishing a RUNTIME enforcement hook (fail-OPEN, wrap body + always `exit 0`, deny only on positive match) from a COMMIT-TIME gate (fail-CLOSED, invoke `|| { exit 1; }` never `if checker`, with a 3-value exit contract 0 clean / 1 violation / 2 parser-or-environment error where zero-units-on-a-non-empty-tree ⇒ 2); appended (not inserted) to avoid renumbering Rule 7's "Rule 1" cross-reference. Cites ICON-0081, ICON-0073/0075. (3) `.context/workflows/task-start-conventions.md` — added a brief ICON-0081 corollary extending the ICON-0072 referenced-artifact-existence check to referenced *conventions/idioms*: confirm via a read-only pass that a claimed repo convention (e.g. `[[wiki-links]]`) actually exists before designing on it. The other lessons (dependency-ordered disjoint-file phasing; isolation scenario-testing + adversarial reviewer battery; fix-the-model-not-the-symptom; dogfooding the seam) stay retro-only as task-specific applications of established patterns. This entry inserted via `append-retrospective-entry.ps1`.

### ICON-0080: GitHub-only conversion (personal install)
- **Avoid**: Dispatching 5 heavy @coder agents in a single batch hit `529 Overloaded` on 4 of them (0 work done) — cap simultaneous heavy sub-agent dispatches to ~3–4 or stagger them. Also under-scoped the skill cross-reference sweep (missed `find-context-template` and `context-maintenance` `datascan-marketplace` defaults), forcing a gap pass — give sweep coders a whole-tree grep mandate, not just a hand-curated file list. And know the pre-commit gates before sequencing commits: the README skill-registration gate (every `skills/<dir>/` needs a README table row) blocked committing the renamed skills until README was updated, and `common-constraints` re-injection re-stages all agent files on every commit — so commit `agents/` and README before `skills/`.
- **Repeat**: Front-load an exhaustive discovery/inventory pass (categorized file map + "if you rename X, also update these" coupling list) before fanning out — it made disjoint-directory parallel sweeps (agents / skills / docs / .claude / context_template) conflict-free in one worktree and gave the reviewer a precise checklist. For wiring a non-git working copy to a fresh GitHub repo: re-root the local baseline onto the empty starter commit (`git reset --soft origin/main` + restore LICENSE) for clean push-ready linear history, and bypass the `.githooks` gate for the baseline-import commit only (`git -c core.hooksPath=.git/hooks commit`) while keeping it active for all real work.
- **Updated**: Promoted the pre-commit-gate-coupling → commit-ordering lesson to `.context/domains/hooks.md` — added a new `## Pre-commit Gate Coupling — Sequencing Multi-Surface Commits (ICON-0080)` section documenting the README skill-registration gate (O-V1, previously undocumented as a planning rule), the common-constraints re-injection re-staging behavior, and the resulting sequence (commit `agents/` + `README.md` before `skills/`); it cross-links the already-documented template-version invariant (ICON-0044/0069) so all three coupled pre-commit gates are co-located. The other lessons (heavy-dispatch overload cap; whole-tree grep mandate for sweeps; non-git→GitHub re-rooting) stay retro-only as task-specific applications. This entry inserted via `append-retrospective-entry.ps1`.

### ICON-0079: presence-test shell convention doc (#46) — placement by token budget; cite by name (both now well-worn)
- **Repeat (placement by ADR-008 token budget)**: The only real call was WHERE to document the `${VAR+x}` presence-test vs `${VAR:-fallback}` convention. Placed it in the on-demand `shell-portability` standard (new Rule 5, beside the `grep -e` rule), NOT in always-loaded `shared/common-constraints.md` — same ADR-008 reasoning as ICON-0072: don't multiply an authoring rule ×9 across always-loaded agent copies for no always-loaded benefit. On-demand shell-authoring gotchas belong with the other shell gotchas in the standard.
- **Repeat (cite by name + keep a point-of-use pointer)**: The icon-init/icon-status inline descriptions (added in ICON-0071 m7) were trimmed to cite the `shell-portability` standard BY NAME (the ICON-0076/0077 by-name rule, dead-ref-safe), while KEEPING the point-of-use `${VAR+x}` bash and a one-line "presence test, not fallback" note. When promoting an inline convention to a canonical doc, leave a brief point-of-use pointer rather than re-explaining it in three places (DRY) or stripping the local warning entirely.
- **Updated**: `.context/standards/shell-portability.md` — added Rule 5 (`${VAR+x}` presence test vs `${VAR:-fallback}`); `skills/icon-init/SKILL.md` and `skills/icon-status/SKILL.md` — trimmed inline convention to a by-name cite of the standard while keeping the point-of-use `${VAR+x}` snippet. Promotion decision: NO promotion — both lessons are clean applications of already-established rules (ADR-008 placement economics; the ICON-0076/0077 cite-by-name rule already promoted to writing-skills), so this stays retro-only. `.context/retrospectives.md` — this entry inserted via `append-retrospective-entry.sh` (10 → 10 at cap, pruned 1 oldest entry: ICON-0069, archived to `retrospectives-archive.md`). This entry completes the #39–#46 open backlog.

### ICON-0078: multimodule root-context parity (#45) — confirm the model behind a flagged asymmetry; renumber every cross-reference
- **Repeat (confirm the conceptual model behind a flagged asymmetry before deciding bug-vs-feature)**: #45 explicitly "needs a design call on whether the multimodule-vs-monorepo root asymmetry is intentional." The answer hinged on the maintainer's repo-type taxonomy — monorepo = aggregation TREE (root→aggregator→leaf, 3 tiers: impl-root+branch+leaf); multi-module = FLAT (root→leaves, 2 tiers: impl-root+leaf, no aggregators) — which the maintainer supplied. With it the call was clear: the asymmetry IS a real gap (a multi-module repo still needs a root `.context/` mapping its leaves for `resolve-repo-context`/@manager routing) but only at the ROOT tier; multi-module correctly gets NO branch/aggregator tier. When an issue asks "is this asymmetry intentional?", the answer usually lives in a conceptual model the maintainer holds — surface/confirm that model before treating the asymmetry as bug-or-feature. (Taxonomy captured in manager memory.)
- **Repeat (reuse a conditional generator across topologies via a param, not a new code path)**: `context-specialist-impl-root` served the flat multi-module root with NO flat-mode flag — its steps already omit cross-project sections when areas are independent, `projects.md` lists leaves directly, and `repo_type: multi-module` propagates to its Step 14 create-iconrc. The fix was a dispatch (mirroring initialize-monorepo Step 5) + a one-line header-prose broadening, not new logic. A generator that conditionally omits sections can adopt a new topology through its existing params.
- **Avoid (renumbering an ordered skill: update EVERY cross-reference)**: inserting the new Step 7 and renumbering (old 7→8, 8→9, 9→10) left one stale "Step 8" cross-reference in Step 4 prose (push-and-MR is now Step 9) — caught by @reviewer, not by any gate. After inserting/renumbering steps in an ordered skill, grep every `Step N` mention (prose, tables, "proceed to Step N", summaries) and update each; the dangling-reference class is invisible to the pre-commit hooks. Pairs with ICON-0077's "match the skill's own structure when extending it" — both are "keep an edited skill internally consistent."
- **Updated**: PROMOTED the renumbering-cross-reference hygiene lesson (now a 2nd data point with ICON-0077) into `skills/writing-skills/SKILL.md` — added a new `### Editing an Existing Numbered Skill` subsection (after the Step/Phase Heading Format section) with two bullets: renumber every `Step N` cross-reference after insert/reorder (ICON-0078, hooks won't catch it), and match the skill's own heading/list structure when borrowing sibling content (ICON-0077, folding in that previously retro-only lesson now that it has a co-occurrence). The other two lessons (confirm-the-model; conditional-generator-reuse) stay retro-only as repeat applications of existing principles. This entry inserted via `append-retrospective-entry.sh`.

### ICON-0077: impl-branch verify step (#44) — match a skill's OWN structure when extending it; pick the role-matching sibling as template
- **Repeat (pick the role-matching sibling)**: Modeled the new verify step on impl-ROOT's Step 15, not impl-leaf's Step 5 — a branch node is a *lighter root* (it generates overview/projects + conditional decisions/architecture/domains), so impl-root's "exists (or explicitly omitted with reason)" conditional checks transferred directly, while impl-leaf's domain-class/standards-snippet/hook-wiring/rules-index/`.gitattributes` checks were correctly excluded as inapplicable to a branch node. When adding a parity step across sibling skills, template from the sibling whose ROLE matches, not merely the first one that has the step.
- **Avoid (match the artifact's OWN structure when extending it)**: The manager spec said "mirror impl-root's `##` per-step heading convention," but impl-branch's own Process is a FLAT numbered list (items 1-8 under a single `## Process`). The coder followed the spec literally and gave the new Verify/Commit their own `##` headings — creating a structural mismatch (flat items 1-8, `##`-headed 9-10) in the same skill. @reviewer caught it; the fix was flat list items 9/10 inside `## Process`. When extending an existing skill or doc, match THAT artifact's own internal structure, even when borrowing a sibling's content — don't impose the sibling's format.
- **Repeat (dead-ref marker: correct use vs the ICON-0076 wrong use)**: Here the `<!-- pre-commit:dead-ref-ok -->` marker was the RIGHT tool — the verify checklist references conditionally-generated OUTPUT paths (`overview.md`, `projects.md`, `decisions/`, `architecture/patterns.md`, `domains/`) that by design never exist in `context_template/`. Contrast ICON-0076, where the marker was the WRONG tool for a permanent live-doc reference (fixed by by-name). The guard was also narrowed to exclude `META.md`/`.gitignore`, which ARE templated and resolve. Marker = conditionally-generated/consumer-only paths; by-name = permanent live docs.
- **Updated**: `skills/writing-skills/SKILL.md` — extended the existing "Cross-Referencing Other Skills" by-name note (the ICON-0076 promotion) with a one-line clarifier on WHEN the dead-ref exemption marker IS correct: conditionally-generated OUTPUT / consumer-only `.context/` paths that by design never exist in `context_template/` (e.g. a verify checklist citing `overview.md`/`projects.md`/`domains/`), with the by-name rule reserved for permanent live-doc refs — a now-2nd data point (ICON-0076 wrong-use + ICON-0077 right-use) making the marker-vs-by-name boundary stable enough to capture. The "match the artifact's own internal structure when extending" lesson stays retro-only (first-occurrence authoring lesson, not yet a distinct promotable axis). The task's primary deliverable (the impl-branch Verify/Commit steps as flat `## Process` items 9/10, with the narrowed dead-ref marker) is committed under the task itself. This entry inserted via `append-retrospective-entry.sh`.

### ICON-0076: security-review skill + secure-coding standard (#43) — new skill over extension; ground standards in own practice; verify a cited ADR's scope
- **Repeat (new skill + explicit role boundary)**: Chose a dedicated `security-review` skill over extending `code-quality-rules` Pass 2 — the latter is consumer-code-framed, `user-invocable:false`, embedded in a 5-pass flow; reviewing ICON's OWN infra has a distinct trigger, audience, and invocation (`user-invocable:true`). The skill's Overview states the boundary explicitly (consumer code → code-quality-rules; ICON's own hooks/scripts → security-review), pre-empting the skill-overlap defect class. When a new concern overlaps an existing skill, prefer a new skill with an explicit boundary statement over contaminating a differently-scoped one.
- **Repeat (ground standards in observed practice)**: The secure-coding standard codified the ~10 conventions ICON's own scripts ALREADY follow — each with a real ADR/task citation (fail-open hooks ICON-0073; Node-builtins ADR-005; `grep -e` ICON-0075; credential placeholders ADR-006) — not generic OWASP boilerplate. A standard grounded in the repo's actual artifacts is verifiable and lived-in; boilerplate gets ignored.
- **Avoid (verify a cited ADR's actual SCOPE before restating it)**: Rule 9 first restated ADR-007 as a blanket "no new `2>/dev/null` in ICON's own scripts" — but ADR-007 decides the OPPOSITE for exactly those files: it scopes the devnull ban to agent-invoked SESSION commands and explicitly EXEMPTS autonomous `.githooks/*` and `skills/*/scripts/*.sh`. The rule miscited its own authority and would have flagged the pre-commit hook's own git-probe suppressions. @reviewer caught it. When a rule or doc cites an ADR, read the ADR's scope section and confirm it actually says what you claim — an ADR is often narrower than its title suggests.
- **Avoid (cross-reference docs by NAME in skills, never by literal `.context/` path)**: Citing `.context/standards/secure-coding.md` by literal path in the SKILL.md tripped the pre-commit dead-`.context/`-reference gate (which expects a `context_template/` mirror) — and the standard is deliberately ICON-local/untemplated. The first fix reached for the gate's exemption marker; the cleaner fix (reviewer) is to reference the standard BY NAME, which matches the writing-skills convention AND sidesteps the gate entirely (no marker, no opt-out). In skill content, reference standards/docs by name, never by literal `.context/` path — especially for ICON-local docs with no template mirror.
- **Updated**: Two promotions this pass (extend-existing, no new files). (1) `.context/standards/skill-decomposition/verify-design-claims-against-artifacts.md` — added a "Cited-ADR-scope corollary (ICON-0076)" plus a fourth occurrence entry and precedent line: an ADR is an artifact too, so restating its authority is a hypothesis until you read its scope section — same hypothesis-not-observation class as restating a gate's requirement (after ICON-0061/0065/0073). (2) `skills/writing-skills/SKILL.md` — added a one-line reinforcement under "Cross-Referencing Other Skills": the by-name rule extends to standards/`.context/` docs, and for ICON-local untemplated docs it is mechanically REQUIRED (a literal `.context/` path trips the dead-ref gate, which has no `context_template/` mirror to resolve against), with the exemption marker reserved for transient refs — not permanent live-doc references. The new-skill-over-extension and ground-standards-in-observed-practice lessons stay retro-only (first/repeat applications, not yet a distinct promotable axis). This entry completes the #39-#43 RFC security backlog. This entry inserted via `append-retrospective-entry.sh`. The task's primary deliverables (the `security-review` SKILL.md, the `secure-coding` standard, and Rule 9's ADR-007 scope correction) are committed under the task itself.
