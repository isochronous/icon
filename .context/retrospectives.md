### ICON-0092: Removing a skill instead of repairing it
- **Avoid**: The `ecological-impact` skill shipped a worked example that disagreed with its own formulas by 1000×, was internally inconsistent on its own units, and quoted a headline rate ~57× the source it cited — and **four consecutive audits read that block without checking the arithmetic**. Prose gets read for shape; numbers embedded in prose get skimmed. A document that asserts computed values nothing in the repo can re-derive has no mechanism by which anyone would notice it is wrong, so the error survives every review indefinitely. When a shipped artifact carries numbers, either make them re-derivable from something committed or expect them to rot unnoticed.
- **Repeat**: Two things. (1) **Removal was cheaper than repair, and choosing it was correct** — the skill was a novelty whose entire value depended on numbers no one could verify, so repairing it would have bought a maintenance obligation for the same unverifiable claims. When triaging a defect, ask what the artifact is worth before asking what the fix costs. (2) Writing the *exclusion reasoning* into `plan.md` Decisions **before** the work — which historical references to leave, and why editing `context_template/` would force a schema bump for a no-op — let the reviewer verify each exclusion against the tree instead of re-deriving the judgement. The reviewer reported no daylight between the claimed and actual state. Recording why something was deliberately *not* touched is as useful to a reviewer as recording what was.
- **Updated**: `.claude/skills/icon-audit/SKILL.md` § Quality Checklist — added a bullet requiring any numeric or computed claim in audited content (worked example, formula, headline rate, cited statistic) to be independently recomputed and checked against its cited source, not read for shape, citing ICON-0092. Judged a distinct axis from the two adjacent lessons: `verify-design-claims-against-artifacts.md` covers a design's claim about gate/hook/config *behavior*, verified before dispatch — verification there is cheap and singular; this lesson is about a *content* claim (an arithmetic/numeric assertion) that resists detection entirely because nothing prompts a reader to check it, unlike ICON-0091's "resisted repeated correction" pattern (multiple passes tried and failed) — here no pass ever tried, across four audits. Promoted to `icon-audit`'s Quality Checklist rather than `.context/standards/` because the concrete failure was audit coverage (four consecutive audits missed it) and the concrete fix is an audit-time check; `icon-audit`'s `## Common Check Patterns` list (the earlier-catching alternative) is duplicated verbatim across all 6 domain briefs, so editing it would mean 6 files — over the one-promotion-target scope for this task. `verify-design-claims-against-artifacts.md` left untouched at 14,999 bytes (not this lesson's axis).

### ICON-0091: Amending a decision record that had gone stale
- **Avoid**: The correction reproduced the defect it was correcting, twice. Round 1 added a *new* false assertion to ADR-005's Decision with a fabricated `(ADR-004)` citation — ADR-004 contains no shell, no parity, no `.ps1`. Round 2's replacement enumerations were incomplete again. Round 3's caveat sentence, written specifically to stop undercounts, contained one. The count of unpaired shell scripts was stated four times and wrong four times (6, 7, 8, against an independently rebuilt 9), and one file — `context_template/context/workflows/post-commit` — evaded five passes, three of which explicitly verified "against the repo." The cause was not carelessness: **"the set of committed scripts" has no reliable representation** — extension misses the extensionless git hooks, a shebang grep matches a markdown file containing fenced shebangs, directory membership misses the `context_template/` copies. When a fact resists repeated careful correction, suspect it is not representable rather than that the last reader was sloppy, and **remove the claim instead of correcting it**. A document should state where things live, not inventory what exists.
- **Repeat**: Three things carried this. (1) Instructing every agent to **rebuild the evidence itself rather than inherit the numbers from its brief** — that is what surfaced that the manager's count was wrong, then that the fix's count was wrong, then that the reviewer's was too. A brief is a lead, not a fact, and saying so in the dispatch is what makes an agent check. (2) Three review rounds, each catching a real defect the previous round introduced, plus one fix agent that caught an error in its own draft before staging — a review that returns "changes requested" twice is working, not failing. (3) When the new `## Amendments` format rule turned out to be violated by two of its own three examples, **the rule was relaxed to match what three independent authors actually wrote**, rather than reformatting the documents to match the rule. A convention whose first uses violate it is the wrong convention.
- **Updated**: `.context/standards/skill-decomposition/verify-design-claims-against-artifacts.md` was the obvious host (8 existing occurrences, same "verify a claim before relying on it" family) but sat at 14,999 bytes — one byte under the 16,000-byte split threshold — so a 9th occurrence plus corollary text would have breached it. Judged a genuinely distinct axis rather than a same-file addition: the existing file's remedy is "verify the claim before relying on it" (verification is possible, merely skipped); this lesson is "verification was attempted repeatedly and still failed, because the claim had no representation to verify against" — the remedy is removing the assertion, not verifying harder. Promoted instead to `skills/context-document-guidelines/SKILL.md § Correcting a stale ADR`, which already carries this task's round-1 lesson ("Verify the replacement, not just the error (ICON-0091)") — added as an adjoining paragraph generalizing beyond ADRs to any `.context/` content doc, following the ICON-0084/ICON-0085 precedent of promoting content-authoring conventions into this skill rather than into `.context/standards/`. `verify-design-claims-against-artifacts.md` left untouched, still 14,999 bytes.

### ICON-0090: Dogfooding upgrade-repo against ICON's own .context
- **Avoid**: `upgrade-repo` encodes the premise "differs from template ⇒ behind template," and that premise is false for any repo that has been maintained since init — including ICON, where 5 of 6 task-plan templates were installed AHEAD of the template and template `phase-completion.md` 1.9 was not a superset of the installed 1.7 (applying it would have deleted the CHANGELOG step, the SHA/PR two-commit rule, and the excludes-aware checklist, and substituted a test-count field into a repo with no test runner). The `iconrc.json` "schema version" compounds it: the field sets are byte-identical across 1.2→1.12, so it is a template-tree *content counter* bumped by the pre-commit invariant, not a schema version, and writing it would assert conformance the repo does not have. **A version-number delta is not evidence of drift, and a template diff is not a direction.** Before applying any template delta, establish which side is ahead — the skill's `!=` comparison cannot express direction and reported 6/6 REVIEW REQUIRED with 0 actionable.
- **Repeat**: Running the skill instead of reading it is what produced the findings. Six agents read this corpus during ICON-0089 and none of them found the `.githooks` file-mode defect (mode 100644 with `core.fileMode=false`, so a fresh macOS/Linux clone runs no hooks at all), the `TEMPLATE_DIR` that resolves to `/context_template` under PowerShell, or the prefix-collision check that misfires on every mature ICON repo by matching the repo's own commits. Executing a procedure end-to-end on a real repo surfaces a class of defect that reading it cannot. Two corollaries that paid off: instructing the agent that **friction is a finding, not an obstacle** — a silent workaround destroys the only evidence a dogfooding run exists to produce — and reporting predictions that did NOT reproduce (the GNU-only `grep -oP` block works fine under Git Bash and is live only on macOS/BSD) rather than quietly dropping them.
- **Updated**: Promoted to `.context/standards/skill-decomposition/verify-design-claims-against-artifacts.md` as an eighth occurrence plus a "Diff-direction corollary (ICON-0090)": a comparison mechanism reporting "differs from X" has not established "behind X" — a version-number field that increments on any tree change is a content counter, not a schema-conformance signal, and writing an unverified value into a downstream repo asserts a conformance never established. Same verify-claim-against-artifact axis as the existing seven occurrences, extended rather than duplicated (`in-task-maintenance.md` considered and rejected — that standard governs mechanical do-it-now obligations, not the interpretive claim that a diff has an inherent direction). File stays under the 16KB split threshold (14,999 bytes).

### ICON-0089: Repo-level plugin audit with a script-offload focus
- **Avoid**: Two of the manager's own dispatch-brief assertions were false and propagated to sub-agents before being caught — the brief told five agents that `ecological-impact`/ADR-004 was a three-cycle carry-forward heading for a fourth (ICON-0059 had closed it; domain 04 verified on disk at `CHANGELOG.md:91`), and a maintenance dispatch cited `.context/domains/github-access.md:11` as stale when that line was already accurate. Both errors came from restating a prior audit's framing rather than checking current state. A manager-relayed finding is a **lead, not a verified fact**: state it as a claim to be checked, and derive prior-cycle status from `CHANGELOG.md` and the working tree rather than from the prior report or session memory. Same verify-don't-assume class as the ICON-0085 harness-capability entry. Separately, the first two commits of this task omitted the `Co-authored-by` trailer that `.context/workflows/commit-conventions.md:123-131` requires for AI-assisted commits — the conventions file was read at close-gate time rather than before the first commit, which is exactly the ordering the manager's own Hardcoded rule forbids.
- **Repeat**: Two dispatch techniques paid for themselves. (1) Instructing the maintenance agent to **sweep each file for the whole staleness class rather than fixing only the cited line** produced 2 of its 4 corrections — including a documented hook matcher (`startup|resume`) that had drifted from what `hooks/hooks.json` actually registers (`startup|resume|clear`) and that nobody had reported. (2) Dispatching the cross-cutting agent with an explicit mandate to **challenge the framing it was given** rather than fill it in: it falsified the manager's "each gate displaces the failure upward" hypothesis by checking whether the gate in question was ever built (it was not — ICON-0015's O-V4 remains unimplemented and the literals were swept by hand), which reframed the cycle's central conclusion from displacement to a detection ceiling. A synthesis agent told to reconcile inputs will reconcile them; one told to attack the premise will test it.
- **Updated**: `.context/standards/skill-decomposition/verify-design-claims-against-artifacts.md` — added a seventh occurrence (ICON-0089) plus a "Relayed-finding corollary (ICON-0089)": a finding restated by an intermediary — a manager's dispatch brief, a prior audit report, session memory — is a hypothesis about current state, not an observation of it. Verify against `CHANGELOG.md`/the working tree before relaying a prior cycle's status as fact, and sweep the surrounding content for the same defect class rather than fixing only the cited line. This is the same axis as the existing ICON-0085 harness-capability corollary, extended from "a design's own restatement" to "a finding relayed through an intermediary" — no new document created. The Co-authored-by trailer lesson stays retro-only (a one-off procedural miss, not a generalizable gap: `commit-conventions.md` already states the rule).

### ICON-0088: `.context/` urgency rule + split exemptions — when a rule needs a special case for a member of its own domain, the boundary axis is wrong
- **Avoid**: Three authoring traps. (1) The split exemption was first drawn on **readership** ("does an agent read this to decide what to do *now*, or to look up what was true *then*?"), which forced a three-property special case (`ENTRY_CAP` + append-script + never-hand-edit) to explain `.context/retrospectives.md` — a file that is genuinely *both* live guidance and a chronological log. That construction shipped a self-contradiction: it asserted the file was "not exempt" (so the 16KB rule applies) *and* "governed instead by `ENTRY_CAP`" (so it doesn't), manufacturing a permanent undischargeable obligation — the file is 31,957 bytes with 0 peer `## ` sections so it cannot satisfy the split gate's ≥3-section precondition, and hand-editing it is separately forbidden. The maintainer's reversal forced the axis to **shape** (organizing axis = time vs topic), which resolved every case, deleted the special case, and — as @reviewer observed — coincides with a precondition the gate already enforces mechanically, where readership was orthogonal to any mechanism. When a rule needs a special case to handle one member of its own domain, the boundary axis is probably wrong: re-axis before adding the carve-out. (2) A parenthetical member enumeration at a **satellite** site (`in-task-maintenance.md`'s "snapshots, closed archives") went stale the instant the exempt class widened — and it was the site an agent consults for exactly that obligation. Satellites point by reference; only the canonical site enumerates. Caught by @reviewer, not by any gate. (3) A 3-cell row written into a 2-column Markdown table silently drops its last cell at render — the entire Correct Action column vanished — and no gate catches column-count mismatch. (4) The `merge=union` driver on `retrospectives.md` can coalesce two branches' independently-correct blank-line separators into one during merge, silently undercounting the `awk RS=""` paragraph parser by one entry against a `grep -c '^### '` heading count — cross-check the two counts after any merge touching the file rather than trusting either alone.
- **Repeat**: (1) **Dogfooding the rule on its own change.** The +64-word `manager.agent.md` edit tripped ADR-008's cumulative-drift re-inventory; discharging it in-task (fresh snapshot superseding the ICON-0033 baseline + ADR-008 Consequences refresh) rather than filing it was both required by the rule being added and the strongest available evidence the rule is real — a task codifying "never defer mechanical maintenance" cannot defer its own. (2) **Resuming ONE @reviewer instance across all seven stamps** rather than spawning a fresh reviewer per round: it carried its independently recomputed ADR-008 figure set forward, caught where later edits destabilized content it had already approved, and at Stamp 6 blocked a maintainer-directed change by showing it contradicted a passage six lines above it. A fresh reviewer each round re-derives the figures and loses the cross-round memory that made those catches possible. (3) **Asking instead of guessing on an ambiguous maintainer instruction** — twice. "This also applies to `context_template` documents" had two readings with materially different work; and a literal reading of "if we distribute the file as part of the plugin, don't split it" would have exempted the eight template-*seeded* files the rule exists for. Surfacing each conflict with the concrete file list let the maintainer rule in one turn instead of shipping the wrong scope.
- **Updated**: Promoted three lessons. (1) New `.context/standards/skill-decomposition/boundary-axis-selection.md` — the re-axis-on-special-case rule (a multi-property carve-out for one domain member signals the wrong axis; prefer an axis coinciding with an already-enforced mechanism over one orthogonal to any), with a `skill-decomposition.md` topic-index row + "When to consult" bullet (already covered by the existing `skill-decomposition` `rules-index.md` parent row — no index change owed). (2) `.context/standards/skill-decomposition/process-doc-sweeps.md` — new `## Satellite Sites Point by Reference; Only the Canonical Site Enumerates` section + 1 anti-rationalization row, topic-index blurb extended to match. (3) `.context/standards/terseness-calibration.md` — added the table column-count companion beside the existing row-count invariant (a row with more cells than its table's column count silently drops the excess at render). The three Repeat items (ADR-008 in-task-maintenance dogfooding, single-reviewer-instance continuity, ask-don't-guess on ambiguous rulings) are task-specific applications of already-established patterns and stay retro-only. `context-graph --check` and `check-rules-index.sh` both exit 0 with all changes staged. (4) `.context/workflows/task-plan/phase-completion.md` — new note under Two-Stage Retrospective Handoff on the `merge=union` coalescing hazard discovered post-insertion: two branches each correctly prepending a retrospective entry can have their added blank-line separators recognized as the same insertion and coalesced into one, undercounting the append script's `awk RS=""` paragraph parser against a `grep -c '^### '` heading count and causing the rolling-log cap to over-retain — not a script defect (verified against the ICON-0086/ICON-0087 merge history), detected by confirming the two counts agree. Repaired the coalesced separator between the ICON-0086 and ICON-0087 entries in this same pass. This entry inserted via `append-retrospective-entry.sh`.

### ICON-0086: Strip Slack from release-plugin — a feature removal is complete only when its dependent references are swept, not just its machinery
- **Avoid**: Nothing notable — a clean, in-scope removal.
- **Repeat**: A feature removal is complete only when every DEPENDENT reference is swept, not just the feature's primary machinery. Deleting the Slack step + `format-slack.sh` was the obvious part; the value was catching the downstream references that would otherwise dangle — the `CLAUDE.md` release-guard "Slack noise" / "post the release announcement" phrasing, the `CONTRIBUTING.md` `SLACK_WEBHOOK_URL` setup paragraph, and the `changelog-discipline.md` "ships to Slack via `format-slack.sh`" rationale clause + a historical anecdote — plus folding the legitimate former Step 11 (marketplace-pickup verify) into Step 9 rather than leaving a numbering gap. A repo-wide grep for the feature's own identifiers (`SLACK_WEBHOOK_URL` / `format-slack` / `AI-Council`), not just the owning skill, is what makes the removal complete. Same whole-tree-grep-mandate class as ICON-0080.
- **Updated**: Nothing to promote — a clean application of the existing whole-tree-grep-sweep discipline (ICON-0080; `skill-decomposition/process-doc-sweeps.md`). Retro-only. This entry inserted via `append-retrospective-entry.sh`.

### ICON-0087: Pin CI GitHub Actions to commit SHAs — the repo's own semgrep rule was silently blocking on a mutable action tag
- **Avoid**: The `security` CI (semgrep SAST) had been RED on every PR and on `main` for many cycles, unnoticed until the user asked. The failing job was the repo's OWN `github-actions-mutable-action-tag` rule firing on `.github/workflows/security.yml` using `actions/checkout@v4` (a mutable tag) — 3 real findings, not a config error, and unrelated to whatever task was in flight. Don't treat a persistently-red CI check as noise or "someone else's problem": `gh run list` + the job log surfaced the real cause in one pass. Read the log before assuming.
- **Repeat**: Pin GitHub Actions to a full 40-char COMMIT SHA (not `@v4`/branch), and VERIFY the SHA against the upstream tag with `gh api repos/<owner>/<repo>/git/refs/tags/<tag> --jq '.object.sha,.object.type'` (dereference an annotated `tag` object to its commit) rather than copying a SHA blind — here the gh-api lookup and the CI-run-resolved SHA agreed (`34e1148…`, type `commit`). A completeness grep (`uses:\s*\S+@(v?[0-9]+|main|master)`) across all workflows confirms none are missed.
- **Updated**: `.context/standards/secure-coding.md` — added Rule 12: CI GitHub Actions must be pinned to a full 40-char commit SHA, never a mutable tag/branch, enforced by the semgrep `github-actions-mutable-action-tag` rule in the `security` workflow, with the `gh api .../git/refs/tags/<tag>` SHA-verification step. Cites ICON-0087.

### ICON-0085: Isolate @architect & @planner — remove a special case by making it uniform; scope-supersede a prior ADR's sub-decision, leaving its prose intact
- **Avoid**: The design doc had an internal inconsistency — §7a PROSE said the @architect downgrade tier is `default`, but the §7b literal site-edit table put it under a `→ basic` column. A coder implemented the site-edit verbatim but FLAGGED the mismatch, and @reviewer caught it (an architecture check isn't Haiku-mechanical → `default`). A design's prose rationale and its literal site-edit values can drift; when implementing a literal site-edit, cross-check it against the design's own prose reasoning and flag drift — the flag is what saved it. Same "an artifact's restatements can drift" class as verify-design-claims-against-artifacts, applied to a design doc's own internals.
- **Avoid**: Shipped a FALSE harness-capability premise that passed BOTH design and @reviewer: the design claimed an isolated sub-agent "cannot clarify live" and replaced @planner's clarification with report-only open questions + a per-agent `ask_user`-precedence override. Wrong — a sub-agent CAN use `ask_user`; isolation is a separate context window + a returned report, NOT loss of the user-input channel. The maintainer caught it post-review. Before designing AROUND a claimed harness limitation ("isolated agents can't X"), VERIFY the harness actually imposes it — do not infer a capability loss from the isolation mechanism. Same assume-vs-verify class as ICON-0073 (verify external-tool behavior by execution, not docs). Corrected on the open PR: isolated @planner/@architect retain `ask_user`; the report open-questions are a COMPLEMENT (assumptions + manager-facing questions); only the inline mid-run manager dialogue genuinely goes away.
- **Repeat**: (1) Removed a special case by making the dispatch model UNIFORM: @architect/@planner were the only two inline agents, so isolating them collapsed the inline/shared-context category to zero members and let the whole concept be removed (one dispatch model, not two) — reusing the existing warmstart template + both agents' existing report contracts + the tier machinery, adding exactly ONE new field (`### Architecture`, the cold-resume slice that standards/domains didn't already cover; ADR-008-lean). (2) Scope-superseding ADR: the isolation model had NO ADR, so created ADR-015 as the record AND scope-superseded only ADR-014's inline carve-out — `**Supersedes**`/`**Superseded-by**` bold-fields both directions with the scope noted ("carve-out only — 014's core stands"), leaving ADR-014's Decision prose INTACT as the historical superseded record. (3) The coupling-ownership pattern (ICON-0084 retro) validated: ICON-0085 owned reconciling the carve-out ICON-0084 introduced, contained in the same files, no churn.
- **Updated**: `skills/context-document-guidelines/SKILL.md` § Related Section (graph seam) → ADR supersede bold-fields — extended the ADR cross-reference convention with the **scope-supersede (partial)** pattern: a new ADR resolving only a PRIOR ADR's specific sub-decision sets `**Supersedes**: ADR-NNN (<scope> only — remainder stands)` on the new ADR and `**Superseded-by**: ADR-MMM (<scope> only)` on the old — the scope noted in BOTH directions — and leaves the partially-superseded ADR's `**Status**`/Decision prose INTACT as the historical record (don't mutate it), citing ICON-0085 (additive shipped-skill doc, no `context_template/` touch → no iconrc bump). Also promoted the false-harness-capability lesson (post-review correctness fix) to `.context/standards/skill-decomposition/verify-design-claims-against-artifacts.md` as a harness-capability corollary — a claimed HARNESS/TOOL limitation ("a sub-agent can't use X", "isolation removes Y") is a hypothesis until verified against the actual harness contract (occurrence count five → six, extending the ICON-0073 external-tool corollary). The other lessons (design-prose-vs-literal-site-edit drift flagged at implementation; removing a special case by making the dispatch model uniform + ADR-008-lean single new field; ICON-0084 coupling-ownership validated) stay retro-only as task-specific applications. This entry inserted via `append-retrospective-entry.sh` (10 → 10 at cap, pruned 1 oldest entry: ICON-0075, archived to `.context/retrospectives-archive.md`).

### ICON-0084: Model-aware delegation — a queued follow-up owns reconciling the assumption it changes; ADRs use bold-fields, not `## Related`
- **Avoid**: An @coder gave ADR-014 a `## Related` footer (links to ADR-004/008), but the ICON-0081 F1 convention routes ADR cross-references to the `**Supersedes**`/`**Superseded-by**` bold-fields + plain-prose `ADR-NNN` refs, NOT a `## Related` footer (that seam is for CONTENT docs — domains/standards/workflows/etc.). The convention was established for the generator (impl-root emission) but not stated where an ADR *author* sees it, so it recurred; @reviewer caught it. Fix: make the ADR cross-reference convention explicit in `context-document-guidelines` (the seam authority) so an ADR author doesn't reach for `## Related`.
- **Repeat**: (1) When the user queued ICON-0085 (isolate @planner/@architect) mid-way through ICON-0084 — which depends on those agents being INLINE (they inherit the session model; the tier rule carves them out) — the coupling was surfaced explicitly and the reconciliation assigned to ICON-0085's natural scope (it already touches the same manager / routing-guide / ADR files), rather than churning in-flight ICON-0084 or discarding its work. The follow-up that CHANGES an assumption owns updating what depended on it. (2) ADR-008-lean placement held: the required field + a one-line rule in the always-loaded manager (+111w) / PM (+70w), the full task→tier mapping in the ON-DEMAND `manager-routing-guide` (+569w, unbudgeted) — a small always-loaded delta right after a terseness pass. (3) Portability (ADR-004) for a harness-specific capability = express the portable CONCEPT (tier), map to model ALIASES (not pinned IDs, so the table rarely churns), and degrade gracefully (advisory where a harness lacks per-subagent model control) so delegation never fails.
- **Updated**: `skills/context-document-guidelines/SKILL.md` § Related Section (graph seam) → ADR supersede bold-fields — added a bullet making the ADR cross-reference convention explicit at the seam authority where an ADR *author* looks: an ADR records supersede relationships in the `**Supersedes**` / `**Superseded-by**` bold-fields and references OTHER ADRs in plain prose as `ADR-NNN`, and does **not** get a `## Related` footer (that footer seam is for content docs only — `domains/`, `standards/`, `workflows/`, `architecture/`, `testing/`, `styling/`), citing ICON-0081 F1 + ICON-0084 (additive shipped-skill doc, no `context_template/` touch → no iconrc bump). The other lessons (a queued follow-up owns reconciling the assumption it changes; ADR-008-lean always-loaded placement of the tier rule; portable-concept + model-aliases + graceful-degradation for a harness-specific capability) stay retro-only as task-specific applications. This entry inserted via `append-retrospective-entry.sh` (10 → 10 at cap, pruned 1 oldest entry: ICON-0074, archived to `.context/retrospectives-archive.md`).

### ICON-0083: Whole-tree terseness pass — for a concision task the guardrail is BEHAVIOR preservation, not verbatim wording; set a word-count target
- **Avoid**: The first pass was far too CONSERVATIVE (manager only −2%) because the guardrail treated anti-rationalization tables, Hardcoded lists, and contract prose as "keep VERBATIM" — which caps a terseness task at the thin unprotected-prose surface and defeats its purpose. The maintainer corrected ("another agent reduced manager 15-20% without impacting behavior"). For a concision/terseness task the guardrail is **BEHAVIOR/RELIABILITY preservation, not verbatim-WORDING preservation**: every rule/trigger/gate/table-row must still BIND identically, but the words — including inside tables and contracts — should shrink hard. A concrete word-count TARGET (~15-20% on verbose always-loaded files) calibrates better than a fidelity metaphor ("JPEG-95") alone, which collapsed into "barely touch anything." Only truly literal string-matched tokens (hook/gate markers, script-parity byte-identity, iconrc schema keys, cited `.context/` paths, `## Related`/`**Supersedes**` seam fields, JSON field names) stay byte-verbatim.
- **Repeat**: (1) Front-loaded inventory → disjoint-batch plan + a per-category do-not-trim guardrail + a gate cheat-sheet let 6 batches run conflict-free in one worktree (ICON-0080 pattern). (2) For instruction content with no test runner, "reliability preserved" is VERIFIABLE: per-batch invariants (discipline-table row-counts unchanged, `description:` frontmatter byte-identical, parity scripts byte-unchanged, a real pre-commit hook run) + two adversarial reviewers CONFIRMING each claim line-by-line and checking meaning-not-just-words. Both approved a 98-file, ~−4,800-word diff with zero behavior drift. (3) Stopped manager at −13.1% rather than de-dup the tiered close-gate restatement (Step 6 + Hardcoded + anti-rat) — surfaced the reliability-reinforcement-vs-size tradeoff to the user instead of unilaterally cutting reinforcing redundancy.
- **Updated**: Promoted the concision-calibration lesson to a NEW standard `.context/standards/terseness-calibration.md` (guardrail = behavior-not-verbatim preservation for a concision task, with the byte-verbatim token classes enumerated; a ~15-20% word-count TARGET, distinct from ADR-008's growth ceiling; the no-test-runner verification invariants — table row-counts, `description:` byte-identity, parity-script identity, gates green, adversarial meaning-not-words review; and the don't-de-dup-reinforcing-redundancy-without-sign-off rule), citing ICON-0083 and ICON-0033/ADR-008 as prior art; added its `rules-index.md` Standards row so it is index-reachable (no `## Related` block → no orphan; ADR-008/ICON-0033 cited by name, dead-ref-safe). The other lessons (front-loaded disjoint-batch planning with a per-category guardrail + gate cheat-sheet; per-batch reliability invariants + dual adversarial reviewers; surfacing the reinforcement-vs-size tradeoff rather than unilaterally cutting) stay retro-only as task-specific applications. This entry inserted via `append-retrospective-entry.sh`.
