# Rules Index

On-demand router for this repo's governing rules across `standards/`, `workflows/`, and `decisions/`. Read the matching row, then open the linked file for the full rule.

> Generated, not templated: regenerate this file by scanning `standards/`, `workflows/`, and `decisions/` — one row per top-level file (a parent row for an indexed sub-directory) and one row per ADR. See `context-specialist-impl-leaf` Step 4.5. The rows below are sentinels showing the schema; replace them with real rows.

## Standards

| Rule | Applies when… | File |
| --- | --- | --- |
| <standard name> | <one-line trigger that routes a reader here> | [standards/<file>.md](standards/<file>.md) |

## Workflows

| Rule | Applies when… | File |
| --- | --- | --- |
| <workflow name> | <one-line trigger that routes a reader here> | [workflows/<file>.md](workflows/<file>.md) |

## Decisions (ADRs)

| ADR | Applies when… | File |
| --- | --- | --- |
| <NNN> | <one-line trigger that routes a reader here> | [decisions/<NNN-slug>.md](decisions/<NNN-slug>.md) |
