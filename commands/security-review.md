---
description: >
  Run ICON's security-review checklist against the current change to ICON's own hooks/scripts (Claude Code only)
---

Invoke the `security-review` skill and run its checklist against the current diff of changes to ICON's own shell/JS infrastructure (hooks, `.githooks/`, scripts), reporting any findings against the `secure-coding` standard.

$ARGUMENTS
